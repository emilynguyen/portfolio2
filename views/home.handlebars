<main id="home-page">
    <!--
    <div id="landing">
        <h1 class="mb-3">{{description}}</h1>

        <h4 class="close-landing-btn opacity-hover glitch right cursor-pointer" glitch="Enter">
            <span></span>Enter
        </h4>
    </div> -->
    <nav>
        <h1 class="mb-1" style="text-align: left">{{description}}</h1>
        <h5 class="hide" style="text-align: left">
            <span class="open-landing-btn opacity-hover cursor-pointer">
                < Back</span>
        </h5><br>
        <br>
        <ul>
            <li class=" active">

                <a href="/design">

                    <h4 glitch="Design"><span></span>Design</h4>
                </a>

            </li>
            <li>

                <a href="/dev">
                    <h4 glitch="Dev"><span></span>Dev</h4>
                </a>

            </li>
            <li>

                <a href="/about" class="meme-btn">
                    <h4 glitch="About" class="glitch"><span></span><span class="meme-btn-text">About</span></h4>
                </a>

            </li>
            <li>

                <a href="/contact">
                    <h4 glitch="Contact"><span></span>Contact</h4>
                </a>
            </li>
        </ul>
    </nav>
</main>



<script>
    /*
    var visited = sessionStorage.getItem('visited');
    console.log("visited: " + visited);
    if (!visited) {
        $('#landing').show();

    }

    $('.close-landing-btn').click(function () {
        $('#landing').hide();
        sessionStorage.setItem('visited', true);
    });

    $('.open-landing-btn').click(function () {
        $('#landing').show();
        sessionStorage.removeItem('visited');
    });
*/

    /*
        $(document).bind("keydown click", function (e) {
            console.log(e.keyCode);
            if (e.metaKey || e.keyCode == 82 && e.metaKey || e.keyCode == 91 && e.metaKey) {
                // alert("refresh");
                return;
            }
            $('#landing').hide();
            sessionStorage.setItem('visited', true);
        });
        */

</script>



<script>
    function nextNav(direction) {
        var $active = $('.active');
        var $next;
        $active.removeClass('active');

        switch (direction) {
            case "down":
                $next = $active.next();
                break;
            case "up":
                $next = $active.prev();
                break;
        }

        if ($next.length) {
            $next.addClass('active');
        } else {

            switch (direction) {
                case "down":
                    $('nav li:first').addClass('active');
                    break;
                case "up":
                    $('nav li:last').addClass('active');
                    break;
            }
        }
    }

    $(document).keydown(function (e) {
        switch (e.keyCode) {

            case 38: // Up arrow    
                nextNav("up");
                break;
            case 39: // Right arrow
                //$('.active a')[0].click();
                break;
            case 40: // Down arrow
                nextNav("down");
                break;
            case 13: // Enter
                $('.active a')[0].click();
                break;
        }
    });

    $('nav li').hover(function () {
        $('.active').removeClass('active');
        $(this).addClass('active');
    });
</script>