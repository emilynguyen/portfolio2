<div class="card mb-md">
  <a href="/{{url}}">
    <img class="card-img mb-xs" src="images/{{icon}}" alt="">
    <h5 class="card-title mb-xs">{{title}}</h5>
  </a>
  <p class="card-description">{{description}}</p>
</div>