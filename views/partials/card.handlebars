<div class="card mb-md">
  <a href="/{{url}}">
    <div style="background-color: {{bg}}" class="card-img-container">
      <img class="card-img" src="images/{{icon}}" alt="">
    </div>
    <h4 class="card-title mb-xs">{{title}}</h4>
  </a>
  <p class="card-description">{{description}}</p>
</div>